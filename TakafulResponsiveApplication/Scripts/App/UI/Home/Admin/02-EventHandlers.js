
//Check for main namespace
if (typeof Home_Admin === 'undefined') {
    // Namespace does not exist, create a new one
    var Home_Admin = {};
}

//Add the event handlers container object to the main namespace
Home_Admin.EventHandlers = {};
















